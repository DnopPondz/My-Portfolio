/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'upload.wikimedia.org',
      'www.supportpal.com',
      'www.whmcs.com',
    ],
  },
};

export default nextConfig;